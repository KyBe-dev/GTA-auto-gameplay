# AGENTS.md

本文件定义 Codex 及其他代码代理在本仓库中的长期开发规则。开始任何工作前，必须先阅读本文件、`docs/ARCHITECTURE.md` 和根目录 `README.md`。

## 1. 项目目标与范围

- 本项目开发一款 Windows 桌面软件，通过识别游戏窗口画面、维护可验证的游戏状态，并模拟普通键盘鼠标输入，辅助完成受支持的 GTA 离线故事模式任务。
- 当前验证对象仅为 GTA V Windows 离线故事模式。
- GTA VI 支持只是未来目标。Windows 版正式发布并完成实际测试前，不得声称已经兼容，也不得根据猜测实现具体 UI、任务或控制参数。
- 不支持 GTA Online、多人模式或任何联网对战场景。
- 本项目不是 Rockstar Games 官方项目，也未获得其认可。

## 2. 不可突破的安全边界

- 不得注入游戏进程，不得读取或修改游戏内存，不得编写或安装内核驱动，不得绕过反作弊、DRM、平台限制或游戏安全机制。
- 输入只能通过正常的 Windows 用户态接口发送，当前首选 Win32 `SendInput`。
- 只有用户明确选择的游戏窗口处于前台且状态可信时，才允许发送输入。
- 窗口失焦、画面捕获失败、状态置信度过低、程序异常或用户触发紧急停止时，必须立即释放全部按键和鼠标按钮并停止自动控制。
- 不得以管理员权限作为默认运行要求。只有某项经过验证的 Windows 功能确实需要提升权限时，才能提出最小权限方案并说明原因。
- 不得实现隐藏运行、规避用户发现、绕过安全警告或阻止用户接管的功能。

## 3. 架构规则

- `docs/ARCHITECTURE.md` 是项目架构、模块边界、状态模型、BYOK 策略和里程碑的主要设计依据。
- 任务逻辑只能调用语义动作，例如 `MoveForward`、`Interact`、`OpenMap`，不得在任务或规划代码中写死具体键位。
- 所有实际键位都必须由 `InputBindingService` 或等价的输入映射层解析，并支持扫描、手动录入、确认和重新校准。
- 从第一个里程碑开始使用统一 `GameState`、带来源和时间的 `Evidence`，以及多帧置信度融合；不得让单帧或单一模型直接覆盖全局状态。
- 大模型只能返回结构化候选状态、目标或恢复建议，不得直接调用输入接口或输出未经验证的连续按键序列。
- GTA 专属逻辑必须通过 `IGameAdapter` 隔离。不得把 GTA V 的 HUD 坐标、任务规则或控制参数写入通用核心模块。
- 优先使用相对坐标、校准配置和可替换适配器，不依赖固定分辨率或固定语言。

## 4. 本地优先与 BYOK

- 实时捕获、基础识别、状态融合、输入控制、紧急停止和安全降级必须在本地运行，不得依赖云端 API 才能启动。
- 云端 Provider 只用于低频、复杂、低置信度的后备判断，并且默认关闭。
- 所有云端 Provider 必须采用 BYOK：每位终端用户自行申请、配置并承担自己的 API Key 和额度。
- 本仓库、安装包、示例配置、测试、日志及 Git 历史中不得包含维护者或终端用户的真实 API Key。
- 不得提供共享维护者 Key、默认公共 API 代理或在用户未配置凭据时偷偷回退到维护者账户。
- 用户凭据只能保存在 Windows Credential Manager 或使用 DPAPI 的用户范围加密存储中，不得明文保存。
- Provider 必须通过统一接口接入、可以替换，并处理未配置、无效 Key、超时、限流、额度耗尽和断网情况。
- 云端失败且本地无法安全判断时，必须暂停或请求人工确认，不得盲目继续。
- 上传云端时只允许发送用户已授权的游戏窗口必要裁剪区域；不得捕获或上传整个桌面。

## 5. 开发方式与变更范围

- 第一次进入仓库时先只读检查项目结构、文档和当前状态，再提出计划；不得在尚未理解架构时直接生成整个项目。
- 每次只实施用户明确授权的里程碑或子任务。不得擅自跨越多个里程碑、扩大产品范围或添加托管服务、收费系统、自动更新等重大功能。
- 如果用户要求“审查”“诊断”或“规划”，只读取和报告，不修改文件。
- 如果用户明确要求“实现”“修复”或“修改”，可以完成范围内的本地改动，并运行安全、非破坏性的验证。
- 遇到会改变架构、安全边界、隐私策略、许可证或公开发布方式的决定时，先列出影响并请求用户确认。
- 不得为迎合现有实现而静默修改 `docs/ARCHITECTURE.md`。架构与实现发生冲突时，先报告冲突。
- 保持变更小而可审查。不要顺便重构无关代码，也不要覆盖用户已有的未提交修改。
- 禁止使用破坏性 Git 操作，例如 `git reset --hard` 或强制推送，除非用户明确指定并确认准确目标。

## 6. 代码与测试要求

- 主程序技术栈为 C#、.NET 8、WPF；除非架构文档或用户明确批准，不更换主技术栈。
- Windows 平台调用与业务逻辑分离，并通过接口注入，以便单元测试和录制画面回归测试。
- 新增功能必须同时考虑正常路径、低置信度、窗口失焦、输入释放、超时、取消和异常恢复。
- 修复缺陷时优先增加能够复现问题的测试。
- 不依赖真实游戏进程完成所有自动化测试；视觉与状态模块应支持使用合法、自制或经过授权的录制夹具测试。
- 运行与变更相关的格式化、构建和测试，并在回复中如实报告命令、结果以及未能执行的验证。
- 不得通过删除测试、降低断言、吞掉异常或写死测试结果来获得通过状态。

## 7. 仓库、隐私与许可证

- `.gitignore` 必须排除 API Key、本地凭据、用户配置、日志、截图、录像、崩溃转储、存档、模型缓存、构建输出和安装包输出。
- 不得提交游戏本体文件、游戏可执行文件、用户存档、账户信息、未经授权的游戏素材或第三方攻略全文。
- 新增依赖、模型、数据集或素材前，记录名称、版本、来源、许可证及是否允许重新分发。
- 在许可证尚未确定时，不得擅自添加许可证或声称项目已经按某种开源许可证授权。
- 不得把“GitHub 仓库公开”描述为“已经正式发布”。可安装版本只有在 M9 检查完成并通过 GitHub Releases 发布后才算正式发布产物。
- 不得在 README、日志或 UI 中声称尚未验证的任务覆盖率、识别准确率、GTA VI 兼容性或完全自动通关能力。

## 8. 文档与交付

- 用户可见行为、配置方式、安全边界或目录结构变化时，同步更新相关文档。
- README 应始终准确反映当前开发状态；开发者预览阶段不得提供误导性的普通用户安装说明。
- 安装程序和桌面快捷方式属于后续发布工作。初始候选为 Inno Setup，脚本放在 `installer/`，并提供可选的当前用户桌面快捷方式。
- 每次任务结束时说明：完成了什么、修改了哪些文件、执行了哪些验证、仍有哪些限制，以及建议的下一小步。

## 9. 指令冲突与不明确事项

- 用户当前任务与仓库文档不一致时，不要自行猜测；指出具体冲突并等待确认。
- 名称、路径、游戏版本、权限、Provider、许可证或验收标准不明确，并且会实质影响实现时，先提出最少数量的具体问题。
- 如果缺少 `docs/ARCHITECTURE.md`、README 或必要的测试资料，应准确列出缺失项，不得虚构其内容或假装已经验证。
